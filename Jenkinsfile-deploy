library("tdr-jenkinslib")

standardDeployJob(
    IMAGE_NAME: "tdr-transfer-frontend",
    TO_DEPLOY: params.TO_DEPLOY,
    ECS_SERVICE: "frontend",
    STAGE: params.STAGE,
    TEST_DELAY_SECONDS: 300
)
