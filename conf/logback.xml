<!-- https://www.playframework.com/documentation/latest/SettingsLogger -->
<configuration>
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} trace_id=%X{trace_id} span_id=%X{span_id} trace_flags=%X{trace_flags} [%level] %logger{15} - %message%n%xException{10}</pattern>
    </encoder>
  </appender>

  <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.mdc.v1_0.OpenTelemetryAppender">
    <appender-ref ref="CONSOLE"/>
  </appender>

  <logger name="play" level="INFO" />
  <logger name="application" level="DEBUG" />
  <logger name="org.keycloak.adapters.KeycloakDeployment" level="WARN"/>

  <root level="INFO">
    <appender-ref ref="OTEL" />
  </root>
</configuration>
